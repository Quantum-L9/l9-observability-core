<!-- L9_RENDERED
template: .cursor/rules/templates/10-domain-cartridge.mdc.template
template_sha256: 9eb1c85dbfa2d44905e1b0cc73c5ab54bb2e0df2b2f4dd386a7e491944406a52
config: plugin-config.yaml
config_sha256: 8c06add1d58d7b23c142621c5dcfd4d68a9f8c123a6ad1960060d43d814e3690
render_id: 0fa8ea39fb1624219a2c292a96139a94b6e9d9ee995ebc73a53465a2111275ba
/L9_RENDERED -->
---
description: "Domain cartridge rule — generated from plugin-config.yaml"
globs: [".cursor/rules/**", "plugin-config.yaml", "scripts/render_cursor_rules.py"]
alwaysApply: false
---
<!-- L9_META role: cursor_rule tags: [domain-cartridge, generated, observability-domain-contract-library] version: 1.2.0 /L9_META -->

# Domain Cartridge Convention

> This file is generated from .cursor/rules/templates/*.mdc.template. Do not edit directly; edit the template or plugin-config.yaml.

## Core Law

Rules must be reusable across repos. Repo-specific values belong in `plugin-config.yaml`, not hardcoded inside `.mdc` files.

## Current Cartridge

- Repo: `l9-observability-core`
- Domain: `observability-domain-contract-library`
- Plugin version: `1.2.0`
- Config SHA: `8c06add1d58d`

## Template Variables

The renderer exposes scalar config keys directly and list keys as bullet/list/json variants:

- `- `uv.lock`
- `pyproject.toml`
- `.l9/runtime-provenance.yaml`
- `scripts/bootstrap_rename.py`
- `scripts/inventory_check.py`
- `scripts/repo_hygiene_audit.py`
- `Makefile`
- `tools/l9_repo/**`` / `- `uv.lock`
- `pyproject.toml`
- `.l9/runtime-provenance.yaml`
- `scripts/bootstrap_rename.py`
- `scripts/inventory_check.py`
- `scripts/repo_hygiene_audit.py`
- `Makefile`
- `tools/l9_repo/**`` / ``uv.lock`, `pyproject.toml`, `.l9/runtime-provenance.yaml`, `scripts/bootstrap_rename.py`, `scripts/inventory_check.py`, `scripts/repo_hygiene_audit.py`, `Makefile`, `tools/l9_repo/**`` / `[
  "uv.lock",
  "pyproject.toml",
  ".l9/runtime-provenance.yaml",
  "scripts/bootstrap_rename.py",
  "scripts/inventory_check.py",
  "scripts/repo_hygiene_audit.py",
  "Makefile",
  "tools/l9_repo/**"
]`
- `- `git push --force`
- `rm -rf .venv`` / `- `git push --force`
- `rm -rf .venv`` / ``git push --force`, `rm -rf .venv`` / `[
  "git push --force",
  "rm -rf .venv"
]`
- `- `make verify`
- `make check-rules`
- `make hygiene-check`` / `- `make verify`
- `make check-rules`
- `make hygiene-check`` / ``make verify`, `make check-rules`, `make hygiene-check`` / `[
  "make verify",
  "make check-rules",
  "make hygiene-check"
]`

## Graph Record

```json
{
  "capabilities": [
    "verify",
    "rename",
    "render-rules",
    "hygiene-check",
    "birth-runner"
  ],
  "capability_evidence": {
    "birth-runner": "scripts/birth-runner/new_repo.py",
    "hygiene-check": "scripts/repo_hygiene_audit.py",
    "rename": "scripts/bootstrap_rename.py",
    "render-rules": "scripts/render_cursor_rules.py",
    "verify": "Repo.mk"
  },
  "data_governance_class": "internal",
  "maintenance_owner": "Quantum-L9/platform",
  "node_type": "plugin_install"
}
```
